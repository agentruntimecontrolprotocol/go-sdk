# delegate

Spec §10. Parent agent calls `jc.Delegate` to spawn a child whose
lease is a subset of its own. The delegate surface is reserved for
v1.1; the harness validates the lease-subsetting rule from §9.4.
